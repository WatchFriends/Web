var webpack = require("webpack"),
    path = require("path"),
    merge = require("webpack-merge");

var config = {
    entry: {
        'main': "./app/ts/main.ts",
    },
    output: {
        
    }
}